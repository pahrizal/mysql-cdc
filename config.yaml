mysql:
  host: localhost
  port: 3306
  user: cdc
  password: cdc
  server_id: 1001
  flavor: mysql  # mysql or mariadb
  version: "5.6"  # Optional: 5.6, 5.7, 8.0, etc. (for documentation/logging)
  use_gtid: true  # Enable GTID replication (MySQL 5.6+)

binlog:
  position_file: .binlog_position
  start_position: 0
  start_timestamp: 0

nats:
  url: nats://localhost:4223
  subject: cdc.mysql
  max_reconnect: 10
  reconnect_wait: 2s

logging:
  level: info

processor:
  enabled: true  # Set to true to enable data transformation
  script: example/transformer.js  # Path to JavaScript transformation script (takes precedence over rules)
  # rules:
    # Example: Transform events for a specific table
    # - database: mydb
    #   table: users
    #   exclude:
    #     - password
    #     - ssn
    #   rename:
    #     email: user_email
    #     name: full_name
    #   add_fields:
    #     source: mysql-cdc
    #     processed_at: "2024-01-01"
    
    # Example: Include only specific fields for all tables in a database
    # - database: mydb
    #   include:
    #     - id
    #     - name
    #     - created_at
    
    # Example: Exclude sensitive fields from all tables
    # - database: ""
    #   table: ""
    #   exclude:
    #     - password
    #     - credit_card
    #     - ssn

